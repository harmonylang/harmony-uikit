{
  "name": "@harmonylang/harmony-uikit",
  "version": "1.0.0",
  "main": "./dist/index.js",
  "license": "MIT",
  "scripts": {
    "compile": "tsc -p .",
    "setup": "yarn install --ignore-scripts",
    "postinstall": "ls && echo $(pwd) && npm run compile && rm tsconfig.json",
    "test": "mocha -r ts-node/register tests/**/*.test.ts"
  },
  "dependencies": {
    "chai": "^4.3.4",
    "fast-glob": "^3.2.7",
    "git-interface": "^2.1.1",
    "mocha": "^9.1.0"
  }
}
